version: '3'

services:
  red:
    build: .
    container_name: linto-red
    image: linto/red
    ports:
      - 127.0.0.1:${HTTP_PORT_RED}:${HTTP_PORT_RED}
    env_file: .env
    networks:
     - internal

networks:
  internal: